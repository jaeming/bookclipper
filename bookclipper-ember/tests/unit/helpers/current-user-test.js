import {
  currentUser
} from 'bookclipper/helpers/current-user';

module('CurrentUserHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = currentUser(42);
  ok(result);
});

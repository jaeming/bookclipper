<div class='row'>
  {{#if authenticated}}
    <p class="right">signed in as {{#link-to 'users.account'}} {{user_name}} {{/link-to}}</p>
  {{else}}
    <p class="right">
      {{link-to 'Sign Up' 'users.new'}} or
      {{link-to 'Log In' 'login'}}
    </p>
  {{/if}}
  <hr>
</div>

<div class="row">
  {{#if authenticated}}
    {{#link-to 'bookmarks.new' class="tiny round button"}}Create New{{/link-to}}
  {{else}}
    <p>Sign in to post</p>
  {{/if}}
</div>

<div class='row'>
  {{outlet}}
</div>

<hr>
<div class="row">
  <small>
    Website thumbnails by <a href="http://www.bitpixels.com/">BitPixels</a>
  </small>
</div>

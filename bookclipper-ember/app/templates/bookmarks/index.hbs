<hr>
{{#link-to 'bookmarks.new' class="tiny round button"}}Create New{{/link-to}}
<br>
<div class="row">
  <div class="columns medium-6">
    {{#each}}
      <h5>{{title}}</h5>
      {{#link-to 'bookmarks.show' this}}
        <img {{bind-attr src='bookmark_img'}} alt='' class='bookmark-img'>
      {{/link-to}}
      <br>
      {{#each hashtags}}
        <small>#{{topic}}</small>
      {{/each}}
      <br>
      <small class='small'>Submitted by {{user.name}}</small>
      <br><br>
    {{/each}}
  </div>
</div>